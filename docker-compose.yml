version: "3.9"

services:
  bot:
    build: .
    image: bot:${TAG}
    container_name: bot
    network_mode: host # name of your network
    pull_policy: never
    env_file:
    - .env

networks:
  default: 
    external: true
    name: host